# A list of file extensions that Nanoc will consider to be textual rather than
# binary. If an item with an extension not in this list is found,  the file
# will be considered as binary.
text_extensions: [ 'adoc', 'asciidoc', 'atom', 'coffee', 'css', 'csl', 'erb', 'haml', 'handlebars', 'hb', 'htm', 'html', 'js', 'less', 'lua', 'markdown', 'md', 'ms', 'mustache', 'php', 'rb', 'rdoc', 'sass', 'scss', 'slim', 'tex', 'txt', 'xhtml', 'xml' ]

prune:
  auto_prune: true
  exclude: [ '.git' ]

data_sources:
  - type: filesystem
    encoding: utf-8
    extra_files:
      - "/**/.htaccess"

deploy:
  staging:
    kind: rsync
    dst: "adobner@sftp.itd.umich.edu:~/staging"
